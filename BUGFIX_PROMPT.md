# Bugfix Prompt Template

Use this when Square v0.1 has an error.

---

Read the project markdown files first.

Fix the following error in Square v0.1.

Rules:

- Do not add new features.
- Do not change the project direction.
- Do not refactor unnecessarily.
- Preserve the current UI structure.
- Fix only the reported bug and directly related code.
- Provide full file contents for every changed file.

Error:

```text
PASTE ERROR MESSAGE HERE
```

Current behavior:

```text
DESCRIBE WHAT HAPPENS HERE
```

Expected behavior:

```text
DESCRIBE WHAT SHOULD HAPPEN HERE
```
